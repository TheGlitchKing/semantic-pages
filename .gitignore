# Dependencies
node_modules/

# Build output
dist/

# Index (rebuilt on demand)
.semantic-pages-index/

# Environment
.env
.env.local

# Claude Code local overrides
.claude/settings.local.json

# Memory is personal — folder is tracked, contents are not
.claude/memory/*
!.claude/memory/.gitkeep
.planning/
.mcp.json

# Skill eval workspaces (test artifacts, never ship in plugin distribution)
skills/*-workspace/

# Local build / test artifacts
*.tgz
.claude/.semantic-pages-update-cache.json
